# Nginx drops inherited environment except TZ and PATH.
# Pass Docker/compose variables through to Passenger.
env BUNDLE_PATH;
env RAILS_ENV;
env DATABASE_HOST;
env DATABASE_PORT;
env DATABASE_USERNAME;
env DATABASE_PASSWORD;
env DATABASE_NAME;
